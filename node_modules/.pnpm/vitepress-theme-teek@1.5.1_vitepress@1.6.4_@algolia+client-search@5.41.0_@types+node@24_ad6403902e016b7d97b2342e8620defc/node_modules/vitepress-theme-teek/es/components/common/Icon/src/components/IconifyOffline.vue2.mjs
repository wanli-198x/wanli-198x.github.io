import { defineComponent, onMounted, useId, createBlock, openBlock, unref } from 'vue';
import { addIcon, Icon } from '@iconify/vue';

var _sfc_main = /* @__PURE__ */ defineComponent({
  ...{ name: "IconifyOffline" },
  __name: "IconifyOffline",
  props: {
    icon: {}
  },
  setup(__props) {
    const props = __props;
    onMounted(() => {
      const id = useId();
      addIcon(`iconify-${id}`, props.icon);
    });
    return (_ctx, _cache) => {
      return openBlock(), createBlock(unref(Icon), {
        icon: __props.icon,
        style: { outline: "none" }
      }, null, 8, ["icon"]);
    };
  }
});

export { _sfc_main as default };
