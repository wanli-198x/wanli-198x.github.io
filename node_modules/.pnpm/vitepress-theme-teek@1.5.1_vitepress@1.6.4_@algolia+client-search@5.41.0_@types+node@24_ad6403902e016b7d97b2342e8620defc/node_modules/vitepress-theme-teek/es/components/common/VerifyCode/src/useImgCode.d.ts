import { Ref } from 'vue';
/**
 * 绘制图形验证码
 * @param width - 图形宽度
 * @param height - 图形高度
 */
export declare const useImageVerify: (width?: number, height?: number) => {
    domRef: Ref<HTMLCanvasElement | undefined, HTMLCanvasElement | undefined>;
    imgCode: Ref<string, string>;
    setImgCode: (code: string) => void;
    getImgCode: () => void;
};
