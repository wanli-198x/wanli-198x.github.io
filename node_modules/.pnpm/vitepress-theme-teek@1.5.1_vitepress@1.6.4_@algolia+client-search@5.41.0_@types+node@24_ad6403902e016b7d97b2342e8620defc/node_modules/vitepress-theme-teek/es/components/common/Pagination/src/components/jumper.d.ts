import { Size } from '../pagination';
import { default as Jumper } from './jumper.vue';
export interface PaginationJumperProps {
    /**
     * 分页大小
     *
     * @default 'default'
     */
    size?: Size;
}
export type PaginationJumperInstance = InstanceType<typeof Jumper>;
