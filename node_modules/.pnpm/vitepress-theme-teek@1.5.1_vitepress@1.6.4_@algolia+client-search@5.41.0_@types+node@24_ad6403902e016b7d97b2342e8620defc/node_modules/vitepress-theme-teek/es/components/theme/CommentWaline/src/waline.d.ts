import { InjectionKey } from 'vue';
import { CommentProvider } from '../../../../config';
export interface WalineInstance {
    el: HTMLElement | null;
    update: (newOptions?: any) => void;
    destroy: () => void;
}
export declare const walineContext: InjectionKey<(el: string | HTMLElement | null, options: CommentProvider["waline"]) => WalineInstance | null>;
