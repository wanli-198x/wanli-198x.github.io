import { defineComponent, createElementBlock, openBlock, normalizeClass, unref, renderSlot } from 'vue';
import 'vitepress';
import { useNamespace } from '../../../../../composables/useNamespace.mjs';
import 'vitepress-theme-teek/theme-chalk/tk-copy-banner.css';
import '../../../../../composables/useZIndex.mjs';

var _sfc_main = /* @__PURE__ */ defineComponent({
  ...{ name: "BorderHighlight" },
  __name: "BorderHighlight",
  props: {
    active: { type: Boolean }
  },
  setup(__props) {
    const ns = useNamespace("border-highlight");
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock(
        "div",
        {
          class: normalizeClass([unref(ns).b(), unref(ns).is("active", Boolean(__props.active))])
        },
        [
          renderSlot(_ctx.$slots, "default")
        ],
        2
        /* CLASS */
      );
    };
  }
});

export { _sfc_main as default };
