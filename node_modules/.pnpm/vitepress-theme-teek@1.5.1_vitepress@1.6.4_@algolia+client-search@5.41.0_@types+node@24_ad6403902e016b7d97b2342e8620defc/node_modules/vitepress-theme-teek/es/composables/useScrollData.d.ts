import { MaybeRefOrGetter, Ref } from 'vue';
export interface UseScrollDataOptions {
    /**
     * 自动滚动间隔时间
     *
     * @default 3000
     */
    intervalTime?: number;
    /**
     * data 发生变化，是否重新加载
     *
     * @default false
     */
    reloadWhenDataChanged?: boolean;
}
/**
 * 定时对数据进行截取，实现滚动
 *
 * @param data 数据
 * @param limit 显示数量
 * @param options 配置项
 */
export declare const useScrollData: (data: MaybeRefOrGetter<any[]>, limit: number, options?: UseScrollDataOptions) => {
    data: Ref<any[], any[]>;
    start: () => void;
    stop: (restore?: boolean) => void;
    restart: () => void;
};
export type UseScrollDataReturn = ReturnType<typeof useScrollData>;
