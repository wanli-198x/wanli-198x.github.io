import { MaybeRefOrGetter } from 'vue';
/**
 * 加载动画
 *
 * @param immediate 是否第一次调用方法时加载动画，（为 false 场景：在初始化页面的时候不需要加载动画，而是在实际操作的时候加载动画）
 */
export declare const useAnimated: (delay?: MaybeRefOrGetter, immediate?: boolean) => {
    start: () => void;
    stop: () => void;
};
