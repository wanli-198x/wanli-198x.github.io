import { default as MarkdownIt } from 'markdown-it';
/**
 * markdown TODO 列表插件
 *
 * 使用：
 * - [ ] 吃饭
 * - [ ] 睡觉
 * - [x] 打豆豆
 *
 * - 可以换成 1. 或 * 或 + 等列表的标记
 */
declare const todoPlugin: (md: MarkdownIt) => void;
export default todoPlugin;
