import { FileContentLoaderData } from 'vitepress-plugin-file-content-loader';
import { TkContentData, PostData } from './types';
import { RequiredKeyPartialOther } from '../../helper';
/**
 * 转换为文章数据
 */
export declare const transformData: (data: FileContentLoaderData) => TkContentData;
/**
 * 转换为各个文章不同类型的数据
 */
export declare const transformRaw: (posts: TkContentData[]) => PostData;
/**
 * 获取文章标题，获取顺序：frontmatter.title > md 文件开头的一级标题 > 文件名
 *
 * @param post 文章数据
 */
export declare function getTitle(post: RequiredKeyPartialOther<TkContentData, "frontmatter" | "relativePath">): any;
/**
 * 获取文章时间，获取顺序：frontmatter.date > 文件创建时间 > 当前时间
 *
 * @param post 文章数据
 * @param srcDir 项目绝对路径
 */
export declare function getDate(post: RequiredKeyPartialOther<TkContentData, "frontmatter" | "relativePath">, srcDir: string): any;
/**
 * 截取 markdown 文件前 count 数的内容
 *
 * @param post 文章数据
 * @param count 截取数量，默认 300
 */
export declare const getCaptureText: (post: TkContentData, count?: number) => string;
