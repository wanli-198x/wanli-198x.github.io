import { Plugin } from 'vite';
import { P as PermalinkOption } from './shared/vitepress-plugin-permalink.BAgS87DY.js';
export { a as Permalink, c as createRewrites } from './shared/vitepress-plugin-permalink.BAgS87DY.js';

/**
 * 默认暴露 2 个插件集
 */
declare function VitePluginVitePressPermalink(option?: PermalinkOption): (Plugin<any> & {
    name: string;
})[];
/**
 * 扫描项目目录，生成 permalink
 */
declare function VitePluginVitePressAutoPermalink(option?: PermalinkOption): Plugin & {
    name: string;
};
/**
 * 1、将 UsePermalink 组件传入 VitePress 的 layout-bottom 插槽
 * 2、将 NotFoundDelay 组件传入 VitePress 的 not-found 插槽
 */
declare function VitePluginVitePressUsePermalink(option?: PermalinkOption): Plugin & {
    name: string;
};

export { PermalinkOption, VitePluginVitePressAutoPermalink, VitePluginVitePressUsePermalink, VitePluginVitePressPermalink as default };
