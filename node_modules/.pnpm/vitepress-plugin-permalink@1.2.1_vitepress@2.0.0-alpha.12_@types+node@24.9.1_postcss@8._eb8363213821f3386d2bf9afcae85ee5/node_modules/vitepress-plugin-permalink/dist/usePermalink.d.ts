/**
 * 监听永久链接跳转
 *
 * @param executeBeforeMountFn 是否执行 beforeMount 阶段，当只要获取 teyGetFilePathByPermalink 函数时，可以设置为 false，避免重复执行 beforeMount 阶段
 */
declare function usePermalink(executeBeforeMountFn?: boolean): {
    startWatch: () => void;
    teyGetFilePathByPermalink: (pathname: string) => string;
};

export { usePermalink as default };
