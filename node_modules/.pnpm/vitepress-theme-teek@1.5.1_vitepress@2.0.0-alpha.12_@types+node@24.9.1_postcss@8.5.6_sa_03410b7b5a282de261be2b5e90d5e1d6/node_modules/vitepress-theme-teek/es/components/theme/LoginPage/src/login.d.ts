import { Component, Ref } from 'vue';
import { TkIconProps } from '../../../common/Icon';
import { Private } from '../../../../config';
/**
 * 获取登录状态的存储键
 */
export declare const getLoginStorageKey: () => {
    siteLoginKey: string;
    pagesLoginKey: string;
    pageLoginKey: string;
    realmLoginKey: string;
};
export declare const loginUrlKeyMap: {
    realm: string;
    toPath: string;
    verifyMode: string;
};
export declare const verifyModeMap: {
    site: string;
    pages: string;
    page: string;
    realm: string;
};
export declare const defaultPrivateConfig: Private;
export type LoginForm = {
    [key in "username" | "password" | "verifyCode"]: LoginFormItem;
};
export interface LoginFormItem {
    model: string;
    focusModel: boolean;
    errorModel: boolean;
    icon: TkIconProps["icon"];
    placeholder: string;
    type: string;
    append?: Component;
    appendModel?: string | Ref<string>;
}
