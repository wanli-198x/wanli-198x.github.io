import _sfc_main from './src/index.vue2.mjs';
import { createVNode, render } from 'vue';

let instance = null;
const createImageViewer = (options) => {
  if (typeof window === "undefined") return;
  const { modelValue = true } = options;
  options.modelValue = modelValue;
  document.body.style.overflow = "hidden";
  const container = document.createElement("div");
  document.body.appendChild(container);
  instance = createVNode(_sfc_main, options);
  render(instance, container);
};

export { _sfc_main as TkImageViewer, createImageViewer };
