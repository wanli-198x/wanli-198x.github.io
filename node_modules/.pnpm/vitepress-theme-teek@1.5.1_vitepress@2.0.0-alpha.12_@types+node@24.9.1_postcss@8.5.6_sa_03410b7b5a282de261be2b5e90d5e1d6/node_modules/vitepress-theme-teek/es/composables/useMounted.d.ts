import { ShallowRef } from 'vue';
export interface UseMountedOptions {
    /**
     * 是否在非 onMounted 下执行
     *
     * @default false
     */
    sync?: boolean;
    /**
     * 是否在 nextTick 后执行
     *
     * @default true
     */
    nexTick?: boolean;
}
export declare const useMounted: (fn?: () => void, options?: UseMountedOptions) => ShallowRef<boolean, boolean>;
