import { MaybeRefOrGetter } from 'vue';
/**
 * mounted 监听事件，unmounted 移出监听事件
 *
 * @param el 监听元素
 * @param event 监听事件
 * @param handler 事件处理函数
 * @param options 监听选项
 * @param condition 监听条件
 * @returns 移除事件的函数
 */
export declare const useEventListener: (target: MaybeRefOrGetter<EventTarget | null | undefined>, event: string, handler: (event: any) => void, options?: AddEventListenerOptions | boolean) => () => void;
