import { defineComponent, createElementBlock, openBlock, unref, normalizeStyle, normalizeClass, renderSlot } from 'vue';
import 'vitepress';
import { useNamespace } from '../../../../composables/useNamespace.mjs';
import 'vitepress-theme-teek/theme-chalk/tk-copy-banner.css';
import { useLocale } from '../../../../composables/useLocale.mjs';
import '../../../../composables/useZIndex.mjs';
import { useTeekConfig } from '../../ConfigProvider/index.mjs';

const _hoisted_1 = ["aria-label"];
var _sfc_main = /* @__PURE__ */ defineComponent({
  ...{ name: "HomeBannerBgPure" },
  __name: "HomeBannerBgPure",
  setup(__props) {
    const ns = useNamespace("banner-bg-pure");
    const { t } = useLocale();
    const { getTeekConfig } = useTeekConfig();
    const getStyle = () => {
      const { pureBgColor = "#28282d" } = getTeekConfig("banner", {});
      return { backgroundColor: pureBgColor };
    };
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock("div", {
        class: normalizeClass(unref(ns).b()),
        style: normalizeStyle(getStyle()),
        "aria-label": unref(t)("tk.homeBanner.bgPureLabel")
      }, [
        renderSlot(_ctx.$slots, "default")
      ], 14, _hoisted_1);
    };
  }
});

export { _sfc_main as default };
