import { defineComponent, useModel, createElementBlock, openBlock, normalizeClass, unref, createElementVNode } from 'vue';
import { ns } from '../namespace.mjs';

var _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "Switch",
  props: {
    "modelValue": { default: false },
    "modelModifiers": {}
  },
  emits: ["update:modelValue"],
  setup(__props) {
    const check = useModel(__props, "modelValue");
    const handleClick = () => {
      check.value = !check.value;
    };
    return (_ctx, _cache) => {
      return openBlock(), createElementBlock(
        "span",
        {
          class: normalizeClass([unref(ns).e("switch"), unref(ns).is("checked", check.value)]),
          onClick: handleClick
        },
        [..._cache[0] || (_cache[0] = [
          createElementVNode(
            "div",
            { class: "action" },
            null,
            -1
            /* CACHED */
          )
        ])],
        2
        /* CLASS */
      );
    };
  }
});

export { _sfc_main as default };
