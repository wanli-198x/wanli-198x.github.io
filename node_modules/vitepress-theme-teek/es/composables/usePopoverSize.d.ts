import { MaybeRefOrGetter, Ref } from 'vue';
export interface UsePopoverSizeOptions {
    placement?: "bottom" | "bottom-start" | "bottom-end" | "top" | "top-start" | "top-end" | "left" | "left-start" | "left-end" | "right" | "right-start" | "right-end";
    /**
     * 偏移量，等价于 `xOffset` 和 `yOffset`
     *
     * @default 0
     */
    offset?: number;
    /**
     * x 偏移量
     *
     * @default 0
     */
    xOffset?: number;
    /**
     * y 偏移量
     *
     * @default 0
     */
    yOffset?: number;
}
export type NumStr = number | string;
/**
 * 获取 Popover 的位置
 *
 * @param trigger 触发器元素
 * @param popover 弹框元素
 * @param options 配置项
 * @remark 返回的 top、right、bottom、left 是基于 body 的坐标，因此 Popover 需要移到 body 下
 */
export declare const usePopoverSize: (trigger: MaybeRefOrGetter<EventTarget | null | undefined>, popover: MaybeRefOrGetter<EventTarget | null | undefined>, options?: UsePopoverSizeOptions) => {
    top: Ref<NumStr, NumStr>;
    right: Ref<NumStr, NumStr>;
    bottom: Ref<NumStr, NumStr>;
    left: Ref<NumStr, NumStr>;
    update: () => void;
};
