import { MaybeRef } from 'vue';
/**
 * 元素出现在屏幕内时，开启过渡效果
 */
export declare const useWindowTransition: (element: MaybeRef<HTMLElement | HTMLElement[] | null>, immediate?: boolean) => {
    start: () => void;
    stop: () => void;
    restart: () => void;
};
/**
 * 使用 IntersectionObserver 监听元素是否可见
 */
export declare const useIntersectionObserver: (observerDom: MaybeRef<HTMLElement | null>, callback: (entries: IntersectionObserverEntry[]) => void, threshold: number) => {
    create: () => void;
    clean: () => void;
};
