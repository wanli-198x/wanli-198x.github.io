import { MaybeRef, Ref } from 'vue';
export declare const varNameList: {
    vpBrand1: string;
    vpBrand2: string;
    vpBrand3: string;
    vpBrandSoft: string;
    vpBg: string;
    vpBgAlt: string;
    vpBgSoft: string;
    vpBgElv: string;
    vpText1: string;
    vpText2: string;
    vpText3: string;
    tkBgColorElm: string;
    tkBgColorMute: string;
};
/**
 * 根据 color 计算其他 var 变量需要的颜色，并直接覆盖这些 var 变量的颜色
 *
 * @param color 主题色
 * @param ignoreList 无视的 var 变量，在覆盖颜色时可以指定哪些 var 变量不覆盖
 *
 * @returnParam start 开启计算逻辑（第一次已自动调用）
 * @returnParam stop 还原 var 变量本身的颜色，并取消 dark 的监听
 * @returnParam update 手动根据 color 再次计算并更新 var 变量
 * @returnParam clear 还原所有 var 变量
 * @returnParam updateSpread 修改 spread 的值
 */
export declare const useThemeColor: (color: MaybeRef<string>, ignoreList?: string[] | (() => string[] | undefined)) => {
    isSpread: Readonly< Ref<boolean, boolean>>;
    start: () => void;
    stop: () => void;
    update: () => void;
    clear: () => void;
    updateSpread: (value: boolean) => boolean;
};
