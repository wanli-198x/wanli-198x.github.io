import { TkContentData, PostData } from './types';
/**
 * Post 空数据，方便定义空对象
 */
export declare const emptyPost: PostData;
/**
 * 过滤非文章页
 * @param posts 所有文章数据
 */
export declare const filterPosts: (posts: TkContentData[]) => TkContentData[];
/**
 * 按置顶和时间排序
 * @param posts 过滤非文章页之后的文章数据
 */
export declare const getSortPostsByDateAndSticky: (posts: TkContentData[]) => TkContentData[];
/**
 * 按时间排序
 * @param posts 过滤非文章页之后的文章数据
 */
export declare const getSortPostsByDate: (posts: TkContentData[]) => TkContentData[];
/**
 * 按分类和标签分组
 * @param  posts 按时间排序之后的文章数据
 */
export declare const getGroupPosts: (posts: TkContentData[]) => PostData["groupPosts"];
/**
 * 获取所有分类和标签
 * @param  groupPosts 按分类和标签分组之后的文章数据
 */
export declare const getGroupCards: (groupPosts: PostData["groupPosts"]) => PostData["groupCards"];
/**
 * 获取文章时间戳
 * @param post 文章数据
 */
export declare const getPostsTime: (post: TkContentData) => number;
/**
 * 文章时间排序
 * @param prev 文章 1
 * @param next 文章 2
 */
export declare const compareDate: (prev: TkContentData, next: TkContentData) => number;
/**
 * 根据年份分组，key 为年份，value 为该年份的文章列表，如 { 2025: [{}, {}], 2024: [{}, {}] }
 * @param posts 文章列表
 */
export declare const groupByYear: (posts: TkContentData[]) => Record<string, TkContentData[]>;
/**
 * 根据年份和月份分组，key 为年份，value 为该年份的月份分组，如：{ 2025: { 01: [{}, {}], 02: [{}, {}] }, 2024: { 01: [], 02: [{}, {}] } }
 * @param posts 文章列表
 */
export declare const groupByYearMonth: (posts: TkContentData[]) => Record<string, Record<string, TkContentData[]>>;
