import { FileInfo } from 'vitepress-plugin-auto-frontmatter';
import { PermalinkRule } from '../interface/plugins';
/**
 * 创建 permalink 永久链接
 *
 * @param permalinkPrefix permalink 前缀
 */
export declare const createSimplePermalink: (permalinkPrefix?: string) => {
    permalink: string;
};
/**
 * 根据规则转换 permalink
 *
 * @param permalink 原始 permalink
 * @param fileInfo 文件信息
 * @param rules 规则数组
 */
export declare const createComplexPermalink: (permalink: string, fileInfo: FileInfo, permalinkRules: PermalinkRule[]) => {
    permalink?: undefined;
} | {
    permalink: string;
} | undefined;
/**
 * 创建 categories 分类列表
 *
 * @param fileInfo 文件信息
 * @param ignore 需要忽略的文件名或目录名
 */
export declare const createCategory: (fileInfo: FileInfo, ignore?: string[]) => {
    categories: string[];
};
/**
 * 处理封面图
 *
 * @param coverImg 原始封面图
 * @param coverList 封面图列表
 * @param enableForceCoverImg 是否强制替换封面图
 */
export declare const createCoverImg: (coverList: string[]) => {
    coverImg: string;
};
