'use strict';

var Vue = require('vue');
var Overlay_vue_vue_type_script_setup_true_lang = require('./Overlay.vue2.js');

const createOverlay = (onClick) => {
  const container = document.createDocumentFragment();
  const app = Vue.createApp(Overlay_vue_vue_type_script_setup_true_lang.default, { onClick }).mount(container);
  return app.$el;
};

exports.createOverlay = createOverlay;
