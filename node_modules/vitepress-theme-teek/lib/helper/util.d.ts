/**
 * 为路径 path 添加站点根路径 base 前缀，等价于 vitepress 的 withBase。Markdown 插件需要用到
 */
export declare const withBase: (base: string, path: string | undefined) => string | undefined;
/**
 * 将字符串的第一个字符大写
 */
export declare const upperFirst: (str: string) => string;
/**
 * 添加单位，如 value 为 16，则返回 16px
 */
export declare const addUnit: (value?: string | number, defaultUnit?: string) => string;
/**
 * 移除单位，如 value 为 16px，则返回 16
 */
export declare const removeUnit: (value?: string | number, defaultUnit?: string) => number | undefined;
/**
 * 获取对象值
 */
export declare const get: (object: Record<string, any>, path: string, defaultValue?: any) => any;
/**
 * 删除 Storage 的数据
 *
 * @param key 键名
 * @param storage 存储类型，如 localStorage, sessionStorage
 * @param vague 是否模糊匹配
 */
export declare const removeStorageItem: (key: string, storage: Storage, vague?: boolean) => void;
